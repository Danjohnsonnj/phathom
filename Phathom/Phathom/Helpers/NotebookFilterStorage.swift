import Foundation

/// `@AppStorage` keys for Notebook filter state (independent of Library).
enum NotebookFilterStorage {
    static let kindKey = "notebook.filter.kind"
    static let statusKey = "notebook.filter.status"
    static let categoryKey = "notebook.filter.category"
}

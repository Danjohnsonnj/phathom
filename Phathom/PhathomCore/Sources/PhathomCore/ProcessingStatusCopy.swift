import Foundation

/// User-facing pipeline detail strings shared by PhathomCore ingest and the app pipeline.
public enum ProcessingStatusCopy {
    public static let embeddingProcessingDetail = "Ready to analyze…"
    public static let pauseStoppingDetail = "Stopping…"
}
